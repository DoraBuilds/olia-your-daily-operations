---
# Flow 05 — Dashboard
# Requires authentication. Run this after logging in manually with a one-time code.
# This flow does not try to automate inbox access.
appId: com.olia.operations
name: "Dashboard — Main Page"

---
- launchApp

- waitForAnimationToEnd

# ── Dashboard assertions (all optional — only pass when authenticated) ─────────
- assertVisible:
    text: "Good morning"
    optional: true
- assertVisible:
    text: "Good afternoon"
    optional: true
- assertVisible:
    text: "Good evening"
    optional: true
- assertVisible:
    text: "Checklists"
    optional: true
- assertVisible:
    text: "Compliance"
    optional: true

# ── Bottom nav (visible when authenticated) ───────────────────────────────────
- assertVisible:
    text: "Dashboard"
    optional: true
- assertVisible:
    text: "Checklists"
    optional: true
- assertVisible:
    text: "Infohub"
    optional: true

- takeScreenshot: "05-dashboard"
